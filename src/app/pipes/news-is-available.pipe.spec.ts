import { NewsIsAvailablePipe } from './news-is-available.pipe';

describe('NewsIsAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new NewsIsAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
